<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotel_management_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".manage_account"></activity>
        <activity android:name=".unbooked_room" />
        <activity android:name=".service_unbooked" />
        <activity android:name=".booked_service" />
        <activity android:name=".booked_room" />
        <activity android:name=".admin" />
        <activity android:name=".room_book" />
        <activity android:name=".service_reservation" />
        <activity android:name=".user_main" />
        <activity android:name=".add_service" />
        <activity android:name=".add_room" />
        <activity android:name=".log_in" />
        <activity android:name=".add_user" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>